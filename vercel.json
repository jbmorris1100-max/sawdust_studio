{
  "buildCommand": "npx expo export --platform web && cp -r supervisor dist/supervisor",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/supervisor", "destination": "/supervisor/index.html" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
